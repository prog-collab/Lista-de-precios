# Datos de clientes y backups locales — nunca subir a este repo público
contactos.csv
productos-backup-*.json

# Planillas de precios: se trabajan localmente. Este repo es público y además
# GitHub Pages sirve todo lo que tenga, así que un .xlsx acá queda descargable
# desde la web por cualquiera.
*.xlsx

# Volcados del catálogo (los seeds con todos los artículos y precios). Los
# schemas de supabase/ SÍ van al repo — es a propósito que esto sea específico
# y no un "supabase/" a secas, que taparía los schemas nuevos.
supabase/articulos_schema.sql
supabase/articulos_seed_*.sql
supabase/migracion_foto_articulo.sql
